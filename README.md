# Liga Leaders Assistant Bot (MVP)

## 1. Что это за проект
Telegram-бот-ассистент для программы обучения руководителей «Лига лидеров».

MVP сфокусирован на рабочем цикле:
`пользователь -> вопрос -> LLM -> ответ -> лог в БД`,
а затем расширен до RAG по загруженным материалам и пользовательским файлам.

## 2. Что умеет бот
- `/start`, `/help`, главное меню.
- Вопросы по обучению с RAG-поиском по материалам.
- Источники сохраняются в логах ответа, но не перегружают сообщение пользователю.
- Сохранение проектного контекста пользователя.
- Загрузка пользовательских файлов `PDF/DOCX/PPTX/TXT`.
- Индексация файлов: извлечение текста -> чанки -> embeddings -> сохранение в БД.
- Безопасная Telegram-админка для статуса, материалов, текстов и статистики.
- Загрузка видео, подкастов, картинок и схем через Telegram `file_id`.
- Логирование вопросов/ответов/ошибок в PostgreSQL.

## 3. Архитектура
```text
Telegram
  ↓
aiogram bot handlers
  ↓
application services
  ↓
PostgreSQL + pgvector
  ↓
OpenAI API (chat + embeddings)
```

Структура:
```text
app/
  bot/
  services/
  rag/
  db/
  llm/
  file_processing/
  config.py
  main.py
data/
migrations/
docs/
Dockerfile
docker-compose.yml
requirements.txt
```

## 4. Требования
- Python 3.11+
- Docker + Docker Compose
- Telegram Bot Token
- OpenAI API key

## 5. Переменные окружения
Скопируй `.env.example` в `.env` и заполни значения:

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_ADMIN_IDS` (список через запятую)
- `DATABASE_URL`
- `OPENAI_API_KEY`
- `OPENAI_CHAT_MODEL`
- `OPENAI_EMBEDDING_MODEL`
- лимиты: `MAX_FILE_SIZE_MB`, `MAX_CONTEXT_CHUNKS`, `MAX_CONTEXT_CHARS`, `MAX_USER_QUESTIONS_PER_MINUTE`

Если OpenAI-ключа пока нет, можно поставить `LLM_PROVIDER=mock`.
В этом режиме бот отвечает тестовым текстом и позволяет проверить Telegram, БД, админов и загрузку файлов.

Пример `DATABASE_URL` для локального запуска:
`postgresql+psycopg://postgres:postgres@localhost:5432/leader_bot`

## 6. Локальный запуск
1. Создай `.env`:
   `cp .env.example .env` (или вручную в Windows).
2. Подними PostgreSQL с pgvector:
   `docker compose up -d postgres`
3. Установи зависимости:
   `pip install -r requirements.txt`
4. Запусти бота:
   `python -m app.main`

Бот запускается в polling-режиме.

## 7. Запуск через Docker
1. Заполни `.env`.
2. Запусти:
   `docker compose up -d --build`
3. Логи:
   `docker compose logs -f bot`

Остановка:
`docker compose down`

## 8. Деплой на Яндекс Cloud VM
Кратко:
- Ubuntu 22.04/24.04
- 2 vCPU
- 4 GB RAM
- 40 GB disk
- Docker Compose
- polling без webhook и HTTPS

Подробная инструкция: `docs/deploy_yandex_vm.md`.

## 9. Админские команды
- `/admin` - безопасное админ-меню.
- `Админ: статус` - проверка БД, LLM, embeddings и RAG.
- `Админ: загрузить материал` - мастер загрузки общего материала.
- `Админ: материалы` - список материалов.
- `Админ: тексты` - редактирование приветствия, помощи и расписания.
- `Админ: статистика` - сводка по пользователям, материалам, вопросам, файлам и ошибкам.
- `Админ: напоминание` - отправка сообщения в привязанный групповой чат программы.
- `Админ: загрузить ICS` - загрузка календарного файла, который прикладывается к уведомлениям.
- `Админ: загрузить медиа` - загрузка видео занятий, аудиоподкастов, картинок и схем через Telegram file_id.
- `Админ: расходы` - приблизительная оценка расходов на VM, LLM и embeddings.
- `/set_group_chat` - привязать групповой чат для напоминаний. Команду нужно отправить внутри группы.
- `/group_chat_status` - проверить, какой групповой чат привязан.
- `/send_reminder текст` - отправить напоминание в привязанный групповой чат.
- `/reindex` - переиндексация документов, оставлена slash-командой без кнопки.
- `/cancel` - сброс состояния.

Доступ проверяется по `TELEGRAM_ADMIN_IDS`.

Инструкция для админов: `docs/admin_guide.md`.

### Групповой чат программы
Бот подготовлен к одностороннему режиму в общем чате:
- в группах бот игнорирует обычные сообщения участников;
- вопросы участников остаются в личном чате с ботом;
- админ может привязать чат командой `/set_group_chat`;
- админ может отправить объявление через `Админ: напоминание` или `/send_reminder текст`.

Плановые напоминания по расписанию пока не включены: их добавим, когда будет финальная программа занятий.

## 10. Пользовательские сценарии
- `Задать вопрос`
- `Материалы программы`
- `Домашние задания`: список ДЗ из БД, карточка задания, помощь ИИ по домашке
- `Расписание Лиги Лидеров`
- `Настройки уведомлений`

Ветки `Уточнить контекст моего проекта` и `Нужна помощь с проектом` есть в коде, но сейчас скрыты из главного меню.

Карта кнопок: `docs/bot_buttons_map.md`.

### Уведомления
Пользователь выбирает одно время уведомлений: `09:00`, `12:00` или `15:00` по Москве.

В MVP бот ежедневно отправляет тестовый текст уведомления. Админ может изменить этот текст через `Админ: тексты` -> `Изменить текст уведомления`.

Если админ загрузил `.ics` через `Админ: загрузить ICS`, бот прикладывает этот календарный файл к уведомлению.

Позже к этому же механизму добавим разные направления уведомлений: оргмоменты, домашние задания, занятия и дедлайны. Время останется одно для пользователя, а сообщений в один день может быть несколько.

## 11. Как загружать материалы
### Пользовательский файл
Сейчас пользовательская загрузка скрыта из меню вместе с веткой проектного контекста.

Когда ветка снова включена:
1. Нажми `Уточнить контекст моего проекта`.
2. Нажми `Загрузить файл с контекстом`.
3. Отправь `PDF/DOCX/PPTX/TXT`.
4. Бот сохранит файл в `data/uploads/{telegram_id}/`.
5. Бот извлечёт текст, создаст чанки и embeddings.
6. После статуса `ready` можно задавать вопросы по файлу.

### Медиа занятий
Видео, подкасты, картинки и схемы в MVP хранятся не бинарником в БД, а как Telegram `file_id`.

Как загрузить:
1. Открой `/admin`.
2. Нажми `Админ: загрузить медиа`.
3. Выбери `Медиа: видео`, `Медиа: подкаст` или `Медиа: картинка`.
4. Выбери привязку: `общий материал` или конкретный `урок/модуль`.
5. Отправь видео, аудиофайл или картинку в Telegram. Название можно указать в подписи.

После этого пользователь увидит файл в `Материалы программы`.

Как пользователь выбирает медиа:
- `Записи и материалы занятий` показывает список загруженных видео кнопками;
- пользователь выбирает конкретную запись, бот отправляет только её;
- `Подкасты на основе занятий` показывает список загруженных аудиоподкастов кнопками;
- `Картинки и схемы` показывает список загруженных изображений кнопками;
- если подкастов нет, бот предлагает сделать текстовую подкаст-выжимку через ИИ.

Полная автогенерация аудиоподкаста из видео пока не включена. Для неё нужен отдельный пайплайн: скачать видео/аудио, сделать транскрипт, собрать сценарий, озвучить TTS, сохранить аудио и выдать пользователю. В MVP используем ручную загрузку готового аудио и текстовую ИИ-выжимку как fallback.

### Глобальный материал (админ)
1. Открой `/admin`.
2. Нажми `Админ: загрузить материал`.
3. Выбери сезон, привязку (`общий материал` или конкретный `урок/модуль`), дату урока и тип материала.
4. Отправь файл.

При загрузке бот сохраняет дату, служебные ключи и теги, например `lesson_key:lesson_1`, `lesson:1`, `module:1`, `date:2026-05-31`, `type:summary`, `scope:general`. Это нужно, чтобы материалы, видео и подкасты можно было связывать с конкретными уроками и находить по запросам вроде `материалы по 31 мая`.

### Как спросить по конкретному материалу
1. Открой `Материалы программы`.
2. Нажми `Записи и материалы занятий`.
3. Найди нужный `id` материала.
4. Напиши вопрос в формате:

```text
материал 1: кратко что в этом файле?
```

Бот будет отвечать только по выбранному материалу.

## 12. Как работает RAG
1. Извлекается текст из `PDF/DOCX/PPTX/TXT`.
2. Текст режется на чанки (около 1200 символов + overlap).
3. Для чанков создаются embeddings.
4. Embeddings сохраняются в `chunks` (`pgvector`).
5. На вопрос пользователя строится embedding вопроса.
6. Ищутся top-k релевантные чанки:
   - global материалы
   - user материалы текущего пользователя
7. Контекст передаётся в LLM.
8. Ответ сохраняется в `messages`, источники добавляются в ответ.

## 13. Ограничения MVP
- Нет интеграции с Moodle.
- Нет SSO и сложной системы ролей.
- Нет интеграций с внутренними корпоративными сервисами.
- Бот знает только вручную загруженные материалы.
- Качество ответа зависит от качества исходных документов.
- Если ответа в материалах нет, бот должен сообщать об этом.

## 14. Troubleshooting
1. Бот не запускается:
   - Проверь `TELEGRAM_BOT_TOKEN` и `OPENAI_API_KEY`.
   - Проверь доступность PostgreSQL.
2. Ошибка БД:
   - Проверь `DATABASE_URL`.
   - Проверь, что поднят контейнер `postgres`.
3. Файл не обрабатывается:
   - Проверь размер (`MAX_FILE_SIZE_MB`) и расширение (`ALLOWED_EXTENSIONS`).
4. Пустые ответы по материалам:
   - Проверь статус документа (`ready`).
   - Выполни `/reindex`.
5. Ошибки модели:
   - Проверь лимиты API и корректность `OPENAI_*` переменных.

Подробный чеклист эксплуатации: `docs/operator_checklist.md`.

## Дополнительно
- SQL-инициализация: `migrations/init.sql`
- Тестовые вопросы: `docs/test_questions.md`
- Карта кнопок: `docs/bot_buttons_map.md`
- Инструкция админа: `docs/admin_guide.md`
- Деплой на Яндекс VM: `docs/deploy_yandex_vm.md`
- Операторский чеклист: `docs/operator_checklist.md`
- Заготовка по видео: `docs/video_infra.md`
